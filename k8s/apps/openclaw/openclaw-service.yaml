apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: openclaw
spec:
  type: ClusterIP
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
    - name: bridge
      port: 18790
      targetPort: 18790
  selector:
    app: openclaw
